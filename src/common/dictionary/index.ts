// this file is just here to simplify the import path within the app

import { categories, frequencies, ICategory, IFrequency } from '../../../../common/dictionary';
export { categories, frequencies, ICategory, IFrequency };
