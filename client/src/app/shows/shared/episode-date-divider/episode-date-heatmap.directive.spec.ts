import { EpisodeDateHeatmapDirective } from './episode-date-heatmap.directive';

describe('EpisodeDateHeatmapDirective', () => {
  it('should create an instance', () => {
    const directive = new EpisodeDateHeatmapDirective();
    expect(directive).toBeTruthy();
  });
});
